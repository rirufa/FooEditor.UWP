<Page
    x:Name="pageRoot"
    x:Class="FooEditor.UWP.Views.FileTypesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FooEditor.UWP"
    xmlns:common="using:FooEditor.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:prism="using:Prism.Windows.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel Margin="30,30,30,30">
        <ListView Grid.Row="1" ItemsSource="{Binding FileTypeCollection}" Name="FileTypeList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DocumentTypeName}" Width="150"/>
                        <Button x:Uid="ShowDetailButton" Command="{Binding ElementName=FileTypeList,Path=DataContext.ShowDetailCommand}" CommandParameter="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
