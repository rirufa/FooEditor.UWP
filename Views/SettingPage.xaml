<Page
    x:Class="FooEditor.UWP.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FooEditor.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:prism="using:Prism.Windows.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel x:Name="FlyoutContent" Margin="30,30,30,30">
            <Button x:Uid="GlobalSettingPageCommand" Margin="0,10,0,0"  Command="{Binding GlobalSettingCommand}"/>
            <Button x:Uid="FileTypesPageCommand" Margin="0,10,0,0" Command="{Binding FileTypeSettingCommand}"/>
            <Button x:Uid="PrintSettingPageCommand" Margin="0,10,0,0" Command="{Binding PrintSettingCommand}"/>
            <Button x:Uid="AboutPageCommand" Margin="0,10,0,0" Command="{Binding AboutPageCommand}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
