<Page
    x:Class="FooEditor.UWP.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FooEditor.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:prism="using:Prism.Windows.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True"
    >

    <StackPanel Orientation="Vertical" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="30,30,30,30">
        <TextBlock Text="{Binding AppName}"  Style="{StaticResource TitleTextBlockStyle}"/>
        <TextBlock Text="{Binding Version}" />
        <TextBlock Text="Copyright FooProject(c)"/>
    </StackPanel>
</Page>
